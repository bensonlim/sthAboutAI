技能：
1. 转skills命令
请将上述 DTO 规范转为 Agent Skill 格式并添加到当前项目的 Skills 中

规则：
1. 读取文件并转换成TRAE规则
帮我把这份后端开发规范文档转化为TRAE规则，要求：
1. 按模块拆分（代码风格/接口规范/安全规范）
2. 对Java文件生效
3. 禁止使用危险函数

文档集：
1.读取类方法并转换成文档集： 读取common模块MtUomRpcClient的方法， 并整理到文档集。


提示词：
1. 读取doc目录下的文档集，为example的查询接口增加一个字段单位编码（uomCode），实体类不需要新增字段，该字段使用跨服务查询，需要先通过物料查询到物料的主单位ID，然后再跨服务查询主单位ID对应的单位编码
2. Example在做保存的时候增加校验：通过物料ID获取物料站点信息，然后对传入的物料ID和站点ID进行匹配，匹配不成功报错。跨服务查询不放在循环里。

